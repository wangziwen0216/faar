# ── 架构：faar（前端）独立部署，请求 ablog（后端 API）──
#
# 开发：npm run dev，由 vite.config.js 把 /api/v1 代理到 ablog（默认 localhost:8081）
# 生产：构建时设置下方变量，或由 faar 容器内 nginx 反代到 ablog（见 nginx.conf）

# 方式 A（推荐，与 faar 同域）：留空或相对路径，由 Vite/nginx 转发到 ablog
# VITE_API_BASE=/api/v1

# 方式 B（前后端不同域名）：浏览器直连 ablog，需在 ablog 开启 CORS
# VITE_API_BASE=http://localhost:8081/api/v1

# Giscus 评论（https://giscus.app）
# VITE_GISCUS_REPO=owner/repo
# VITE_GISCUS_REPO_ID=
# VITE_GISCUS_CATEGORY_ID=
